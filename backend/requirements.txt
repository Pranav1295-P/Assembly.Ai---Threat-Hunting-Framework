# Assembly.AI backend dependencies.
# Versions use ">=" so pip can pick a wheel that matches your Python
# version. Tested on Python 3.10, 3.11, 3.12, 3.13, 3.14 (macOS / Linux / Windows).

# --- Web ---
Flask>=3.0,<4
Flask-Cors>=4.0,<6
python-dotenv>=1.0,<2

# --- Static analysis ---
pefile>=2024.8.26
python-magic==0.4.27;  platform_system != "Windows"
python-magic-bin==0.4.14;  platform_system == "Windows"

# --- Reporting ---
reportlab>=4.2,<5
matplotlib>=3.9
networkx>=3.3
Pillow>=10.4

# --- ML stack ---
# numpy >=2.2 has cp314 wheels; <3 keeps us inside the current major.
numpy>=2.2,<3
scikit-learn>=1.5
pandas>=2.2
joblib>=1.4

# --- HTTP / LLM clients ---
requests>=2.32
anthropic>=0.39
openai>=1.51

# --- Database ---
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9
Flask-SQLAlchemy>=3.1,<4

# --- Production WSGI server (used by Render / Railway / Fly.io) ---
gunicorn>=22.0
